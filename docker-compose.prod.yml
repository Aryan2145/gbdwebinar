version: "3.8"

# Production — EC2 + RDS
# Usage: docker-compose -f docker-compose.prod.yml up -d
services:
  app:
    build: .
    ports:
      - "80:3000"
    env_file:
      - .env
    restart: unless-stopped
